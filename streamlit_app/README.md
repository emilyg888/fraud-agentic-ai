# Streamlit App

The Streamlit app is a thin business analyst UI over `airlab_fraud_agentic_ai.dashboard.service`.

## Rule

Do not add fraud investigation, governance, registry, or workflow logic directly in the UI.
All business operations must stay in the service layer or lower.
